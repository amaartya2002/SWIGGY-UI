## SWIGGY-UI

- Used parcel as a bundler
- creeated this gitignore using - npx gitignore Node
- added scripts for different builds

## React.createElement()

- React api for creating a react element.
- React.createElement() is a JS object.
- But when we render this JS object on to the DOM it becomes htmlElement

## ReactDOM.createRoot()

- React api to create root which will be responsible for rendering items on the DOM.
